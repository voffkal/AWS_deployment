# api
flask==1.0.2


# schema validation
marshmallow==3.19.0

# Set this to the previous model version
# vve-regression-model==0.0.4 is installed from a GitHub Release asset
# (see the 'Fetch model package' step in .circleci/config.yml), not from
# a package index - pip cannot pin a release asset by version here.